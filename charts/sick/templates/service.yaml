apiVersion: v1
kind: Service
metadata:
  name: {{ include "sick.fullname" . }}
  labels:
    {{- include "sick.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sick.selectorLabels" . | nindent 4 }}
